meta {
  name: login user
  type: http
  seq: 2
}

get {
  url: {{base_url}}/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "kshitij.shahi55@gmail.com"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
